let Search = {
  initialize () {
    let initialSearchParams = {
       ...gon.search,
       numGuests: parseInt(gon.search.numGuests),
    };

    SearchStateStore.initialize(initialSearchParams);

    ReactDOM.render(
      <ReactRedux.Provider store={SearchStateStore.storeInstance}>
        <SearchMain />
      </ReactRedux.Provider>,
      document.getElementById("search")
    );
  }
};
