let Search = {
  initialize () {
    SearchStateStore.initialize(gon.search);

    ReactDOM.render(
      <ReactRedux.Provider store={SearchStateStore.storeInstance}>
        <SearchMain />
      </ReactRedux.Provider>,
      document.getElementById("search")
    );
  }
};
